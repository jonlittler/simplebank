# GitHub

## Workflows
```bash
git status
git remote add origin https://github.com/jonlittler/simplebank.git
git branch -M main
git push -u origin main

# create a branch
git checkout -b ft/docker
git status

```
